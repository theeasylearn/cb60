// write a program reverse given string
// car
// rac
#include <stdio.h>
#define SIZE 128
void main()
{
    // declare string(1d array of character)
    char line[SIZE];
    int word = 1, index = 0;
    printf("enter your name");
    gets(line); // without space janvi
    fflush(stdin);     //
    
}